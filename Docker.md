# Docker 部署

当前仓库不再提供绑定外部品牌镜像的部署说明。

如需使用 Docker，可基于本项目源码自行构建镜像，并挂载以下目录持久化数据：

- `/var/lib/mysql`：数据库文件
- `/var/www/html`：网站文件、配置和上传文件

部署后访问：

- 前台：`http://服务器IP:端口/`
- 后台：`http://服务器IP:端口/admin/`

默认后台账号密码为 `admin` / `123456`，首次登录后请立即修改。
